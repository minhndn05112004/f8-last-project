# URL của backend API (không có dấu / ở cuối)
# Dev: để trống hoặc để localhost
# Production: Vite tự build với giá trị này khi deploy
VITE_API_URL=http://localhost:5000

# Nếu muốn override socket URL riêng (tùy chọn)
# VITE_SOCKET_URL=https://f8-last-project.onrender.com/chat
